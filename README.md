# HouseMate

Flask API + Angular SPA + MongoDB

## Run backend
cd backend
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python run.py

## Run frontend
cd frontend/housemate-frontend
ng serve
